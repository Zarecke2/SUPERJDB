<form method="post" action="{{ path('journal_de_bord_delete', {'id': journal_de_bord.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ journal_de_bord.id) }}">
    <button class="btn">Delete</button>
</form>
